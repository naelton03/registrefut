import 'package:flutter/material.dart';

class AppShadows {
  static final md = [
    BoxShadow(
      color: Colors.black.withOpacity(0.08),
      blurRadius: 12,
      offset: const Offset(0, 4),
    ),
  ];
}
